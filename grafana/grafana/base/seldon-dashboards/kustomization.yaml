apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# namespace: openshift-operators

commonLabels:
  opendatahub.io/component: "true"
  component.opendatahub.io/name: grafana

generatorOptions:
 disableNameSuffixHash: true

configMapGenerator:
- name: seldon-predictions-analytics-dashboard
  files:
  - ./predictions-analytics-dashboard.json

resources:
- predictions-analytics-dashboard.yaml
